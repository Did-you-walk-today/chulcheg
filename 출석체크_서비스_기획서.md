# 출석 체크 서비스 기획서

> 학교 와이파이(IP) 기반 접속 제어 + 출/퇴근·외출/복귀 기록 서비스
> 버전 0.1 (초안) · 작성 목적: 개발 착수 전 범위·구조 합의

---

## 1. 서비스 개요

학교 와이파이에 연결된 상태에서만 접속 가능한 웹 기반 출석 관리 서비스.
가입한 인원이 로그인 후 **출근 / 퇴근 / 외출 / 복귀**를 직접 기록한다.
물리적 위치(학교 내부 네트워크) 안에 있을 때만 동작하게 만들어, 원격에서의 대리·허위 출석을 1차적으로 차단하는 것이 핵심 컨셉이다.

**한 줄 정의:** "학교 와이파이에 붙어 있어야만 누를 수 있는 출퇴근 버튼."

---

## 2. 핵심 컨셉 / 차별점

- **IP 기반 접속 제어**: 학교망 공인 IP가 아니면 서비스 진입 자체가 차단됨 (로그인 화면도 안 보임).
- **심플한 상태 기반 UX**: 복잡한 메뉴 없이, 현재 내 상태에 따라 누를 수 있는 버튼만 노출.
- **모바일 우선**: 실사용은 거의 휴대폰. 큰 버튼 / 한 화면 완결형.
- **빠른 기록**: 로그인 → 버튼 한 번 → 기록 완료까지 최소 동선.

---

## 3. 기술 스택

| 영역 | 선택 | 비고 |
|------|------|------|
| 프레임워크 | **Next.js (App Router)** | 크로스 플랫폼(웹/모바일 웹) 단일 코드 |
| 배포 | **Cloudflare** (Workers + OpenNext, 또는 Pages) | 엣지에서 클라이언트 IP 확보 용이 |
| DB | **Cloudflare D1** (SQLite 기반) *권장* | Cloudflare 네이티브, 무료 티어 충분 |
| ORM | Drizzle ORM | D1/엣지 호환, 타입 안전 |
| 인증 | 자체 세션(서명 쿠키 / JWT) | 외부 서비스 없이 가볍게 |
| 비밀번호 해시 | Web Crypto API(PBKDF2) | 엣지 런타임 호환 (bcrypt는 엣지에서 무거움) |

### DB 대안
Cloudflare에 종속되기 싫다면:
- **Turso** (SQLite, 무료 티어) — D1과 거의 동일한 사용감, 엣지 친화적
- **Supabase** (Postgres, 무료 티어) — 인증·실시간 기능 내장이지만 이번 요구엔 과함
- **Neon** (Postgres, 무료 티어) — 서버리스 Postgres

> 이번 프로젝트는 "빠르고 심플 + Cloudflare"라는 조건이라 **D1을 1순위**로 추천. 별도 인프라 없이 끝남.

---

## 4. 기능 요구사항 명세

### 4.1 회원가입
- **필수 입력: 이름, 전화번호, 비밀번호**
  - 이름: 1~20자
  - 전화번호: 숫자만, 형식 검증 (예: `010-0000-0000` 또는 `01000000000`)
  - 전화번호는 **로그인 ID 겸 고유키(UNIQUE)** 로 사용 → 중복 가입 차단
- 비밀번호: 로그인 인증용 (요구사항엔 명시 없지만 "로그인" 기능상 필요)
- 가입 시점에도 **학교 IP 안에서만** 가능

> ❓ 결정 필요: 비밀번호 없이 "전화번호+이름"만으로 인증할지, 비밀번호를 둘지.
> 보안/대리출석 방지를 생각하면 **비밀번호 권장**. (아래 6장 확인 사항 참고)

### 4.2 로그인
- 전화번호 + 비밀번호
- 성공 시 세션 쿠키 발급 (HttpOnly, Secure)

### 4.3 출퇴근 / 외출 복귀 (상태 머신)
사용자의 "오늘 상태"를 기록 로그로부터 도출한다.

**상태 정의**
| 상태 | 의미 |
|------|------|
| 미출근 | 오늘 아직 출근 기록 없음 |
| 근무중 | 출근함, 자리에 있음 |
| 외출중 | 외출 기록 후 복귀 전 |
| 퇴근 | 오늘 퇴근 처리 완료 |

**허용되는 상태 전이**
```
미출근 ──[출근]──▶ 근무중
근무중 ──[외출]──▶ 외출중
외출중 ──[복귀]──▶ 근무중
근무중 ──[퇴근]──▶ 퇴근
```

**전이 규칙(검증)**
- 출근 없이 외출/복귀/퇴근 불가
- 외출 없이 복귀 불가
- 하루 1회 출근 (재출근 차단 — 정책 협의 필요)
- 퇴근 후 추가 액션 차단 (재출근 허용 여부는 정책)
- 각 액션은 **버튼 노출 단계에서부터** 현재 상태에 맞는 것만 보이게 (서버에서도 재검증)

### 4.4 기록 조회 (선택)
- 본인 출퇴근 이력 (날짜별)
- (확장) 관리자: 전체 인원 당일 출석 현황 대시보드

---

## 5. IP 기반 접속 제어 (핵심 기능 상세)

### 동작 방식
1. Next.js **middleware** 가 모든 요청에서 클라이언트 IP를 읽는다.
   - Cloudflare 헤더 `cf-connecting-ip` 사용 (엣지에서 신뢰 가능한 실제 IP)
2. 허용 IP 목록(환경변수 `ALLOWED_IPS`, CIDR 범위 지원)과 대조한다.
3. 일치하지 않으면 **403 안내 페이지**로 차단: "학교 와이파이에 연결된 상태에서만 이용할 수 있습니다."
4. 일치하면 정상 진입 (로그인/가입/기록 모두 가능).

### 허용 정책 (확정)
- **학교 IP 범위 전체를 승인한다.** 학교에서 나가는 모든 공인 IP 대역을 화이트리스트에 포함시킨다.
- 단일 IP가 아니라 **대역(CIDR)** 단위로 등록한다. (예: `203.0.113.0/24` 처럼 학교가 보유한 전체 범위)
- 캠퍼스/건물별로 여러 대역이 있다면 **모두** `ALLOWED_IPS` 에 추가한다.
- IPv6로 나가는 경우가 있으면 해당 IPv6 대역도 함께 등록한다.
- ⏳ **실제 IP 대역 값은 학교 현장 확인 후 입력 예정** (확인되면 `ALLOWED_IPS` 환경변수에 반영).

```
[요청] ─▶ Cloudflare ─▶ middleware: cf-connecting-ip 확인
                          │
              허용 IP?  ──┴── 예 ─▶ 서비스 정상 동작
                          │
                          └── 아니오 ─▶ 차단 페이지(403)
```

### ⚠️ 반드시 사전 확인 (프로젝트 성패 좌우)
- [ ] **학교 와이파이의 공인(외부) IP가 고정인가?** 유동이면 화이트리스트가 주기적으로 깨짐.
- [ ] 공인 IP가 **하나인가, 여러 개/대역인가?** (캠퍼스/건물별로 다를 수 있음) → CIDR로 관리
- [ ] IPv6로 나가는 경우가 있는가? (IPv4/IPv6 둘 다 등록 필요할 수 있음)
- [ ] 확인 방법: 실제 학교 와이파이에 붙은 상태에서 `whatismyip` 류 접속 → 나온 IP 등록

> IP가 유동이거나 제어 불가하면, 대안으로 **위치(GPS) 기반 검증**이나 **현장 QR/일회성 코드** 방식을 보조로 검토. 다만 이번 범위에선 IP 방식으로 진행하고, 위 확인이 안 되면 일정에 영향.

### 한계 (인지하고 진행)
- VPN/테더링 등으로 학교망을 우회할 가능성은 존재 (완벽한 위변조 방지는 아님).
- 조직 내부용 출석 관리로는 "충분히 실용적인" 1차 통제 수준.

---

## 6. 데이터 모델 (초안)

### `users`
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| name | TEXT | NOT NULL |
| phone | TEXT | NOT NULL, UNIQUE |
| password_hash | TEXT | NOT NULL |
| role | TEXT | DEFAULT 'user' (user/admin) |
| created_at | TIMESTAMP | DEFAULT now |

### `attendance_logs` (이벤트 로그)
| 컬럼 | 타입 | 제약 |
|------|------|------|
| id | INTEGER | PK |
| user_id | INTEGER | FK → users.id |
| type | TEXT | CLOCK_IN / CLOCK_OUT / STEP_OUT / RETURN |
| created_at | TIMESTAMP | DEFAULT now |

- 현재 상태는 **당일 로그의 마지막 type 으로 도출** (별도 상태 테이블 불필요 → 단일 진실 원천).
- 조회 성능 필요 시 `(user_id, created_at)` 인덱스 추가.

---

## 7. 화면 구성 (라우트)

| 경로 | 화면 | 설명 |
|------|------|------|
| `/` | 대시보드 | 현재 상태 카드 + 상황별 액션 버튼 |
| `/login` | 로그인 | 전화번호 + 비밀번호 |
| `/register` | 회원가입 | 이름 + 전화번호 + 비밀번호 |
| `/history` | 내 기록 | 날짜별 출퇴근 이력 (선택) |
| `/blocked` | 접속 차단 | 학교망 외부에서 진입 시 안내 |
| `/admin` | 관리자 (확장) | 전체 인원 당일 현황 |

---

## 8. 프론트엔드 디자인 방향 (이번 테스트 핵심)

이번 라운드에서 집중해서 다듬을 영역.

- **모바일 우선 / 한 화면 완결**: 스크롤 없이 상태와 버튼이 한눈에.
- **상태 카드**: 현재 상태를 큼직하게 (미출근/근무중/외출중/퇴근), 색상으로 구분
  - 미출근(회색) · 근무중(초록) · 외출중(주황) · 퇴근(파랑/남색)
- **상황별 버튼만 노출**: 미출근→[출근만], 근무중→[외출][퇴근], 외출중→[복귀] …
- **큰 탭 타겟**: 손가락으로 누르기 쉬운 버튼 사이즈.
- **즉각 피드백**: 기록 시 토스트/애니메이션으로 "출근 완료 09:12" 확인.
- **군더더기 없는 톤**: 정보 밀도 낮게, 한국어 라벨 명확하게.

> 디자인 시안은 별도로 1~2개 스타일을 잡아서 비교해보는 걸 추천 (예: 미니멀 화이트 vs 다크/카드형).

---

## 9. 개발 단계 (제안 로드맵)

1. **0단계 — 사전 확인**: 학교 공인 IP 확정 (5장 체크리스트). *여기서 막히면 설계 변경.*
2. **1단계 — 골격**: Next.js 프로젝트 + Cloudflare 배포 파이프라인 + D1 연결.
3. **2단계 — IP 미들웨어**: 접속 제어 + 차단 페이지.
4. **3단계 — 인증**: 회원가입 / 로그인 / 세션.
5. **4단계 — 출퇴근 로직**: 상태 머신 + 기록 + 검증.
6. **5단계 — 프론트 디자인**: 대시보드/버튼/상태 카드 다듬기 (이번 집중 영역).
7. **6단계(확장)**: 기록 조회 / 관리자 현황.

---

## 10. 확인이 필요한 사항 (Open Questions)

1. **학교 공인 IP 고정 여부** — 가장 중요 (5장). → **정책 확정: 학교 IP 범위 전체를 승인.** 실제 대역 값만 현장 확인 후 입력 예정.
2. **비밀번호 도입 여부** — 로그인을 비번 기반으로 할지, 전화번호+이름만으로 할지.
3. **재출근 정책** — 하루 한 번만? 퇴근 후 재출근 허용?
4. **관리자 기능** — 이번 범위에 포함할지, 다음으로 미룰지.
5. **DB 최종 선택** — D1로 확정할지(권장), 다른 무료 DB로 갈지.
6. **시간대/기준 날짜** — 자정 기준으로 "오늘" 끊는지 (야간 근무 케이스 있는지).

---

*다음 단계: 위 확인 사항(특히 1·2번) 결정되면, 화면 와이어프레임 → 프론트 디자인 시안 → 구현 순으로 진행.*
